diff --git a/node_modules/@safe-global/protocol-kit/dist/src/Safe.js b/node_modules/@safe-global/protocol-kit/dist/src/Safe.js
index 1234567..abcdefg 100644
--- a/node_modules/@safe-global/protocol-kit/dist/src/Safe.js
+++ b/node_modules/@safe-global/protocol-kit/dist/src/Safe.js
@@ -1057,7 +1057,7 @@ var utils_2 = require("./utils");
  * @throws "No signer provided"
  * @throws "There are X signatures missing"
  */
-async executeTransaction(safeTransaction, options) {
+async executeTransaction(safeTransaction, options, chain) {
     if (!__classPrivateFieldGet(this, _Safe_contractManager, "f").safeContract) {
         throw new Error('Safe is not deployed');
     }
@@ -1093,7 +1093,7 @@ async executeTransaction(safeTransaction, options) {
         const txResponse = await __classPrivateFieldGet(this, _Safe_contractManager, "f").safeContract.execTransaction(signedSafeTransaction, {
             from: signerAddress,
             ...options
-        });
+        }, chain);
         return txResponse;
     }
*** End of file ***
diff --git a/node_modules/@safe-global/protocol-kit/dist/src/contracts/BaseContract.js b/node_modules/@safe-global/protocol-kit/dist/src/contracts/BaseContract.js
index 1234567..abcdefg 100644
--- a/node_modules/@safe-global/protocol-kit/dist/src/contracts/BaseContract.js
+++ b/node_modules/@safe-global/protocol-kit/dist/src/contracts/BaseContract.js
@@ -1,6 +1,7 @@
 "use strict";
 var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
 };
+const { BrowserProvider, Provider: ZKProvider, Contract: ZKContract, Signer, utils } = require('zksync-ethers');
 var _BaseContract_instances, _BaseContract_resolveAddress;
 Object.defineProperty(exports, "__esModule", { value: true });
 const viem_1 = require("viem");
@@ -76,7 +77,7 @@ class BaseContract {
     }
     this.chainId = chainId;
     this.safeVersion = safeVersion;
     this.contractAddress = resolvedAddress;
+    this.chainId = chainId.toString();
     this.contractAbi =
         customContractAbi ||
             deployment?.abi || // this cast is required because abi is set as any[] in safe-deployments
@@ -87,7 +88,10 @@ class BaseContract {
 }
 async init() {
-    this.wallet = await this.safeProvider.getExternalSigner();
+    this.wallet = await this.safeProvider.getExternalSigner();
+    if (this.chainId && (this.chainId === '531050104' || this.chainId === '50104')) {
+        this.contract = new ZKContract(this.contractAddress, this.contractAbi, this.wallet);
+    }
 }
*** End of file ***
diff --git a/node_modules/@safe-global/protocol-kit/dist/src/contracts/Safe/v1.4.1/SafeContract_v1_4_1.js b/node_modules/@easy-global/protocol-kit/dist/src/contracts/Safe/v1.4.1/SafeContract_v1_4_1.js
index 1234567..abcdefg 100644
--- a/node_modules/@safe-global/protocol-kit/dist/src/contracts/Safe/v1.4.1/SafeContract_v1_4_1.js
+++ b/node_modules/@safe-global/protocol-kit/dist/src/contracts/Safe/v1.4.1/SafeContract_v1_4_1.js
@@ -1,5 +1,6 @@
 "use strict";
 var __importDefault = (this && this.__importDefault) || function (mod) {
+const { BrowserProvider, Provider: ZKProvider, Contract: ZKContract, Signer, utils } = require('zksync-ethers');
 };
 Object.defineProperty(exports, "__esModule", { value: true });
 const actions_1 = require("viem/actions");
@@ -33,7 +34,7 @@ class SafeContract_v1_4_1 extends SafeBaseContract_1.default {
     * @param deploymentType - Optional deployment type for the contract. If not provided, the first deployment retrieved from the safe-deployments array will be used.
      */
     constructor(chainId, safeProvider, isL1SafeSingleton, customContractAddress, customContractAbi, deploymentType) {
-        const safeVersion = '1.4.1';
+        const safeVersion = '1.4.1';
         const defaultAbi = types_kit_1.safe_1_4_1_ContractArtifacts.abi;
         super(chainId, safeProvider, defaultAbi, safeVersion, isL1SafeSingleton, customContractAddress, customContractAbi, deploymentType);
         /**
@@ -200,7 +201,42 @@ class SafeContract_v1_4_1 extends SafeBaseContract_1.default {
  * @returns Transaction result.
  */
-async execTransaction(safeTransaction, options) {
+async execTransaction(safeTransaction, options, chain) {
+    // Sophon paymaster configuration
+    const PAYMASTER_ADDRESSES = {
+        "531050104": '0x98546B226dbbA8230cf620635a1e4ab01F6A99B2',
+        "50104": '0x98546B226dbbA8230cf620635a1e4ab01F6A99B2',
+    };
+
+    // Check if Sophon chain and use paymaster
+    if (chain && PAYMASTER_ADDRESSES[chain.chainId]) {
+        const safeAddress = await this.getAddress();
+        
+        // Setup paymaster parameters
+        const paymasterParams = utils.getPaymasterParams(
+            PAYMASTER_ADDRESSES[chain.chainId],
+            {
+                type: 'General',
+                innerInput: new Uint8Array(),
+            }
+        );
+
+        // Encode transaction data
+        const calldata = this.contract.interface.encodeFunctionData('execTransaction', [
+            safeTransaction.data.to,
+            safeTransaction.data.value,
+            safeTransaction.data.data,
+            safeTransaction.data.operation,
+            safeTransaction.data.safeTxGas,
+            safeTransaction.data.baseGas,
+            safeTransaction.data.gasPrice,
+            safeTransaction.data.gasToken,
+            safeTransaction.data.refundReceiver,
+            safeTransaction.encodedSignatures()
+        ]);
+
+        // Create zkSync-compatible signer
+        const browserProvider = new BrowserProvider(this.safeProvider.provider);
+        const signer = Signer.from(
+            await browserProvider.getSigner(),
+            Number(chain.chainId),
+            new ZKProvider(chain.rpcUri.value, {
+                name: chain.chainName,
+                chainId: Number(chain.chainId)
+            })
+        );
+
+        // Execute EIP712 transaction with paymaster
+        const tx = await signer.sendTransaction({
+            type: utils.EIP712_TX_TYPE,
+            from: options.from,
+            to: safeAddress,
+            data: calldata,
+            customData: {
+                gasPerPubdata: utils.DEFAULT_GAS_PER_PUBDATA_LIMIT,
+                paymasterParams,
+            }
+        });
+
+        return tx;
+    }
+
+    // Standard execution path
     const gasLimit = options?.gasLimit ||
         (await this.estimateGas('execTransaction', [
             safeTransaction.data.to,
@@ -228,7 +264,7 @@ async execTransaction(safeTransaction, options) {
         (0, types_1.asHex)(safeTransaction.encodedSignatures())
     ];
     return (0, utils_1.toTxResult)(this.runner, await this.write('execTransaction', args, { ...options, gasLimit }), options);
-}
+}
 }
 exports.default = SafeContract_v1_4_1;
*** End of file ***
diff --git a/node_modules/@safe-global/protocol-kit/dist/src/contracts/Safe/v1.3.0/SafeContract_v1_3_0.js b/node_modules/@safe-global/protocol-kit/dist/src/contracts/Safe/v1.3.0/SafeContract_v1_3_0.js
index 1234567..abcdefg 100644
--- a/node_modules/@safe-global/protocol-kit/dist/src/contracts/Safe/v1.3.0/SafeContract_v1_3_0.js
+++ b/node_modules/@safe-global/protocol-kit/dist/src/contracts/Safe/v1.3.0/SafeContract_v1_3_0.js
@@ -1,5 +1,6 @@
 "use strict";
 var __importDefault = (this && this.__importDefault) || function (mod) {
+const { BrowserProvider, Provider: ZKProvider, Contract: ZKContract, Signer, utils } = require('zksync-ethers');
 };
 Object.defineProperty(exports, "__esModule", { value: true });
 const actions_1 = require("viem/actions");
@@ -33,7 +34,7 @@ class SafeContract_v1_3_0 extends SafeBaseContract_1.default {
     * @param deploymentType - Optional deployment type for the contract. If not provided, the first deployment retrieved from the safe-deployments array will be used.
      */
     constructor(chainId, safeProvider, isL1SafeSingleton, customContractAddress, customContractAbi, deploymentType) {
-        const safeVersion = '1.3.0';
+        const safeVersion = '1.3.0';
         const defaultAbi = types_kit_1.safe_1_3_0_ContractArtifacts.abi;
         super(chainId, safeProvider, defaultAbi, safeVersion, isL1SafeSingleton, customContractAddress, customContractAbi, deploymentType);
         /**
@@ -200,7 +201,42 @@ class SafeContract_v1_3_0 extends SafeBaseContract_1.default {
  * @returns Transaction result.
  */
-async execTransaction(safeTransaction, options) {
+async execTransaction(safeTransaction, options, chain) {
+    // Sophon paymaster configuration
+    const PAYMASTER_ADDRESSES = {
+        "531050104": '0x98546B226dbbA8230cf620635a1e4ab01F6A99B2',
+        "50104": '0x98546B226dbbA8230cf620635a1e4ab01F6A99B2',
+    };
+
+    // Check if Sophon chain and use paymaster
+    if (chain && PAYMASTER_ADDRESSES[chain.chainId]) {
+        const safeAddress = await this.getAddress();
+        
+        // Setup paymaster parameters
+        const paymasterParams = utils.getPaymasterParams(
+            PAYMASTER_ADDRESSES[chain.chainId],
+            {
+                type: 'General',
+                innerInput: new Uint8Array(),
+            }
+        );
+
+        // Encode transaction data
+        const calldata = this.contract.interface.encodeFunctionData('execTransaction', [
+            safeTransaction.data.to,
+            safeTransaction.data.value,
+            safeTransaction.data.data,
+            safeTransaction.data.operation,
+            safeTransaction.data.safeTxGas,
+            safeTransaction.data.baseGas,
+            safeTransaction.data.gasPrice,
+            safeTransaction.data.gasToken,
+            safeTransaction.data.refundReceiver,
+            safeTransaction.encodedSignatures()
+        ]);
+
+        // Create zkSync-compatible signer
+        const browserProvider = new BrowserProvider(this.safeProvider.provider);
+        const signer = Signer.from(
+            await browserProvider.getSigner(),
+            Number(chain.chainId),
+            new ZKProvider(chain.rpcUri.value, {
+                name: chain.chainName,
+                chainId: Number(chain.chainId)
+            })
+        );
+
+        // Execute EIP712 transaction with paymaster
+        const tx = await signer.sendTransaction({
+            type: utils.EIP712_TX_TYPE,
+            from: options.from,
+            to: safeAddress,
+            data: calldata,
+            customData: {
+                gasPerPubdata: utils.DEFAULT_GAS_PER_PUBDATA_LIMIT,
+                paymasterParams,
+            }
+        });
+
+        return tx;
+    }
+
+    // Standard execution path
     const gasLimit = options?.gasLimit ||
         (await this.estimateGas('execTransaction', [
             safeTransaction.data.to,
@@ -228,7 +264,7 @@ async execTransaction(safeTransaction, options) {
         (0, types_1.asHex)(safeTransaction.encodedSignatures())
     ];
     return (0, utils_1.toTxResult)(this.runner, await this.write('execTransaction', args, { ...options, gasLimit }), options);
-}
+}
 }
 exports.default = SafeContract_v1_3_0;
*** End of file ***
diff --git a/node_modules/@safe-global/protocol-kit/dist/src/contracts/SafeProxyFactory/v1.4.1/SafeProxyFactoryContract_v1_4_1.js b/node_modules/@safe-global/protocol-kit/dist/src/contracts/SafeProxyFactory/v1.4.1/SafeProxyFactoryContract_v1_4_1.js
index 1234567..abcdefg 100644
--- a/node_modules/@safe-global/protocol-kit/dist/src/contracts/SafeProxyFactory/v1.4.1/SafeProxyFactoryContract_v1_4_1.js
+++ b/node_modules/@safe-global/protocol-kit/dist/src/contracts/SafeProxyFactory/v1.4.1/SafeProxyFactoryContract_v1_4_1.js
@@ -1,5 +1,6 @@
 "use strict";
 var __importDefault = (this && this.__importDefault) || function (mod) {
+const { BrowserProvider, Provider: ZKProvider, Contract: ZKContract, Signer, utils } = require('zksync-ethers');
 };
 Object.defineProperty(exports, "__esModule", { value: true });
 const SafeProxyFactoryBaseContract_1 = __importDefault(require("../../../contracts/SafeProxyFactory/SafeProxyFactoryBaseContract"));
@@ -26,7 +27,7 @@ class SafeProxyFactoryContract_v1_4_1 extends SafeProxyFactoryBaseContract_1.
     * @param deploymentType - Optional deployment type for the contract. If not provided, the first deployment retrieved from the safe-deployments array will be used.
      */
     constructor(chainId, safeProvider, customContractAddress, customContractAbi, deploymentType) {
-        const safeVersion = '1.4.1';
+        const safeVersion = '1.4.1';
         const defaultAbi = types_kit_1.safeProxyFactory_1_4_1_ContractArtifacts.abi;
         super(chainId, safeProvider, defaultAbi, safeVersion, customContractAddress, customContractAbi, deploymentType);
         /**
@@ -50,6 +51,45 @@ class SafeProxyFactoryContract_v1_4_1 extends SafeProxyFactoryBaseContract_1.de
     async createProxyWithNonce(singleton, initializer, saltNonce, options = {}) {
         const txData = this.encode('createProxyWithNonce', [singleton, initializer, saltNonce]);
         const converted = this.convertOptions(options);
+        
+        // Sophon paymaster configuration for Safe creation
+        const PAYMASTER_ADDRESSES = {
+            "531050104": '0x98546B226dbbA8230cf620635a1e4ab01F6A99B2',
+            "50104": '0x98546B226dbbA8230cf620635a1e4ab01F6A99B2',
+        };
+
+        // Check if Sophon chain and use paymaster
+        if (PAYMASTER_ADDRESSES[this.chainId]) {
+            // Setup paymaster parameters
+            const paymasterParams = utils.getPaymasterParams(
+                PAYMASTER_ADDRESSES[this.chainId],
+                {
+                    type: 'General',
+                    innerInput: new Uint8Array(),
+                }
+            );
+
+            // Create zkSync-compatible signer
+            const browserProvider = new BrowserProvider(this.safeProvider.provider);
+            const signer = Signer.from(
+                await browserProvider.getSigner(),
+                Number(this.chainId),
+                new ZKProvider(this.safeProvider.getExternalProvider().url, {
+                    name: 'Sophon',
+                    chainId: Number(this.chainId)
+                })
+            );
+
+            // Execute Safe creation with paymaster
+            const tx = await signer.sendTransaction({
+                type: utils.EIP712_TX_TYPE,
+                to: this.contractAddress,
+                data: txData,
+                from: converted.account,
+                customData: {
+                    gasPerPubdata: utils.DEFAULT_GAS_PER_PUBDATA_LIMIT,
+                    paymasterParams,
+                }
+            });
+
+            // Return proxy address from tx receipt
+            const receipt = await tx.wait();
+            const proxyCreationEvent = receipt.logs.find(log => log.topics[0] === '0x4f51faf6c4561ff95f067657e43439f0f856d97c04d9ec9070a6199ad418e2351bd355458e');
+            const proxyAddress = '0x' + proxyCreationEvent.data.slice(26);
+            return proxyAddress;
+        }
+
         return (0, utils_1.toTxResult)(this.runner, await this.write('createProxyWithNonce', [singleton, initializer, saltNonce], converted), options);
     }
 }
*** End of file ***

